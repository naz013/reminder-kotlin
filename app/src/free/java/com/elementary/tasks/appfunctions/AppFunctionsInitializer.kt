package com.elementary.tasks.appfunctions

/** No-op in the free build - AppFunctions (Gemini integration) is a PRO-only feature and the
 * `:appfunctions` module isn't even on the free flavor's classpath. */
object AppFunctionsInitializer {
  fun init() {}
}
